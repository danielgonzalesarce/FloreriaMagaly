rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /arrangements/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == "daniel.gonzales.a@tecsup.edu.pe";
    }
  }
}
